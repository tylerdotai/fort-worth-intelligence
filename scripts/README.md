# Scripts

Utility scripts for turning discovery inputs into structured source records.

## Current scripts

- `normalize-discovery-urls.js`
  - reads `data/raw/discovery_urls.txt`
  - outputs `data/raw/discovery_urls.normalized.json`
  - provides a starter structure for validation work

## Usage

```bash
node scripts/normalize-discovery-urls.js
```
